<p>
  <b>Name:</b>
  <%=h @user.name %>
</p>

<% if User.find(session[:user_id]).is_admin? %>
<p>
  <b>Email:</b>
  <%=h @user.email %>
</p>
<% end %>

<% if session[:user_id].to_i == params[:id].to_i  || User.find(session[:user_id]).is_admin? %>
<%= link_to 'Edit', edit_user_path(@user) %>
<% end %>

<%= link_to 'Back', :controller => 'stories', :action => 'index' %>