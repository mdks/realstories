%h1
  Editing comment
- form_for([@comment.story, @comment]) do |f|
  = f.error_messages
  %p
    = f.label :body
    %br
    = f.text_area :body
  %p
    = f.submit 'Update'
= link_to 'Back', @comment.story
